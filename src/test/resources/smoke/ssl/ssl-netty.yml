cassandra:
  clusterName: "smoke-cluster"
  contactPoints:
    type: static
    contactPoints: ["localhost"]
  port: 9142
  ssl:
    type: netty
    provider: openssl
    clientAuth: optional
    sessionCacheSize: 1000
    sessionTimeout: "10 minutes"
    trustCertChainFile: src/test/certs/cassandra.pem
