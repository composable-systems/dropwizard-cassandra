cassandra:
  clusterName: "smoke-cluster"
  contactPoints:
    type: static
    contactPoints: ["localhost"]
  reconnectionPolicy:
    type: exponential
    baseDelay: 10 milliseconds
    maxDelay: 1 second