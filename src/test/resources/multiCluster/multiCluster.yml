cassandra1:
  clusterName: "cluster1"
  keyspace: "ks1"
  contactPoints:
    type: static
    contactPoints: ["localhost"]

cassandra2:
  clusterName: "cluster2"
  keyspace: "ks2"
  contactPoints:
    type: static
    contactPoints: ["localhost"]
